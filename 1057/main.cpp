#include <iostream>
#include <cmath>

using namespace std;

int main()
{
    int N, K, L;
    cin >> N >> K >> L;
    while (N > 0) 
    {
        int mid = N / 2;
        if (K<mid && L>mid) {

        }
    }
}