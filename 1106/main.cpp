#include <iostream>
#include <vector>

using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    int c, n;
    cin >> c >> n;
    int dp[20];
    vector<pair<int, int>> vec;
    for (int i = 0; i < n; i++) {

    }
}