#include <iostream>

int main() 
{
    char string[1001];
    scanf("%s", string);
    char* a=string, *b=string;

    for (int i = strlen(string) / 2; i < strlen(string); i++) {
        while (1) {

        }
    }
}
