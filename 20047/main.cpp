#include <iostream>
#include <string>

using namespace std;

int dp[10000][3];

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    int n;
    string s, t;
    int c1, c2;
    cin >> n;
    cin >> s >> t;
    cin >> c1 >> c2;
    char co1 = s[c1], co2 = s[c2];

    for (int i = 0; i < n; i++) {
        if(s[i] != t[i])

    }
    
}