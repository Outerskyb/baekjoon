#include <iostream>


using namespace std;

int main() 
{
	ios::sync_with_stdio(false);
	cin.tie(0);

	int n;
	cin >> n;
	bool map[1000] = { 0 };
	for (int i = 0; i < n; i++) {
		int num, int s, int b;
		int a = num / 100, b = (num - a * 100) / 10, c = num % 10;
		if (s == 3) {
			cout << 1; return 0;
		}
		else if (s == 2) {
			for (int j = 0; j < 1000; j++) {
				
			}
		}
	}
}