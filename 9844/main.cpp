#include "bits/stdc++.h"
#define max(a,b) (((a)>(b))?(a):(b))

using namespace std;

int map[500][500];
int dp[500][500];

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);

    for(int i = 0;  i < n ; i++){
        for(int j = 0; j < n;j++){
            
        }
    }
}